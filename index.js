const VueCompiler = require('vue-template-compiler')
const fs = require('fs')


 // md.forEach(markdown => {
      //   if (markdown.name) {
      //     str += `# name \n ${markdown.name}\n`
      //   } else if (markdown.methods) {
      //     str += `# methods \n ${markdown.methods}\n`
      //   }
      // })